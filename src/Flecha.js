function Flecha() {
  /* function sumar(a,b){
        return a+ b;
    } */

  const sumar = (a, b) => a + b;
  console.log(sumar(5, 10));

  const saludar = () => 'Hola mundo';
  console.log(saludar());

  return <h1>Funciones de Flecha</h1>;
}

export default Flecha;
